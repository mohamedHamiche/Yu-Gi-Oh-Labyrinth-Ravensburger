Pour télécharger la bibliothèque SDL2 permettant d'executer le jeu:

```shell
sudo apt-get install libsdl2-image-dev

```
Update & upgrade:
```shell
sudo apt-get update && sudo apt-get upgrade
```
Optionnel:
```shell
sudo apt-get install libsdl2-image-dev
```